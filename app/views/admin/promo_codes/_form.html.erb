<% if @promo_code.errors.any? %>
    <div class="alert-message error" data-alert><a class="close" href="#">×</a>
      <p>Whoops! Check your form for possible errors</p>
      <p>
        <% @promo_code.errors.full_messages.each do |msg| %>
            <%= msg %><br>
        <% end %>
      </p>
    </div>

<% end %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.label :value %>
<%= f.text_field :value %>

<%= f.label :active %>
<%= f.check_box :active %>

<div>
  <%= f.submit "Save", :class => 'btn btn-primary' %> or
  <%= link_to "Cancel", admin_promo_codes_path %>
</div>
